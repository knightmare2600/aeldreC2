[pytest]
testpaths = tests
markers =
    c2_integration: full C2 protocol tests (require Docker + Wine, set C2_INTEGRATION=1)
    wine: tests that require Wine in PATH (auto-skipped when Wine absent)
addopts = -ra --tb=short
